// Placeholder for Email Service (could use Nodemailer or SendGrid)

export const sendNotification = async (email, subject, message) => {
  console.log(`Sending email to ${email}: [${subject}] ${message}`);
  // Implementation for sending email
};
